import { Component, Input } from '@angular/core';

@Component({
  selector: 'app-activate-code',
  templateUrl: './activate-code.component.html',
  styleUrls: ['./activate-code.component.sass']
})
export class ActivateCodeComponent {
  code: string = "";
}
